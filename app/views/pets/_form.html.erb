<%= form_with(model: pet) do |f| %>
  <%= render 'shared/error_messages', object: pet %>

  <div class="mb-3">
    <%= f.label :name, "Pet Name", class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :owner_id, "Owner", class: "form-label" %>
    <%= f.collection_select :owner_id, Owner.all, :id, :last_name, { prompt: "Select an Owner" }, { class: "form-select" } %>
  </div>

  <div class="mb-3">
    <%= f.label :breed, class: "form-label" %>
    <%= f.text_field :breed, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :date_of_birth, class: "form-label" %>
    <%= f.date_field :date_of_birth, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :weight, class: "form-label" %>
    <%= f.number_field :weight, step: :any, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :photo, "Profile Photo", class: "form-label" %>

    <% if pet.photo.attached? && pet.persisted? %>
      <div class="mb-2">
        <span class="d-block small text-muted mb-1">Current photo:</span>
        <%= image_tag pet.photo, style: "width: 100px;", class: "img-thumbnail" %>
      </div>
    <% end %>

    <%= f.file_field :photo, class: "form-control" %>
    <div class="form-text">Accepted formats: JPEG, PNG, WebP. Max size: 5MB.</div>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>